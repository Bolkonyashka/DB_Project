@{
    var locCode = Request.QueryString["locality"].ToString();

    var db = Database.Open("master");
    var sql = @"SELECT count(FULLNAME) FROM [BD_Project_DB].[dbo].streets WHERE CODE LIKE '" + locCode + "%' ";
    var data = db.QueryValue(sql);
    Json.Write(data, Response.Output);
    db.Close();
}
